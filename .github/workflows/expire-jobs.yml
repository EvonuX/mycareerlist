name: Daily cron job
on:
  schedule:
    - cron: '0 0 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Daily cron job
        run: |
          curl --request GET \
          --url 'https://mycareerlist.com/api/expire'
